@model IEnumerable<cm.Models.StatusDeal>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.StatusDealName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Weight)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatusDealName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Weight)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StatusDealId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StatusDealId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StatusDealId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
